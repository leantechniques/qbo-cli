# NOTE (Lean Techniques fork): kept only for parity with upstream merges — do
# not run it here. There is no release workflow in this fork by design, and the
# publishing targets below are upstream's (voska/homebrew-tap, voska/scoop-bucket),
# so `goreleaser release` from this fork would push fork-built artifacts at
# someone else's tap. It would also produce macOS binaries built CGO_ENABLED=0,
# which silently fall back to a passphrase-prompting file keyring. LT ships a
# signed, notarized .pkg from the qbo-cli-deploy repo instead.
version: 2

builds:
  - main: ./cmd/qbo
    binary: qbo
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^ci:"

brews:
  - name: qbo
    directory: Formula
    homepage: "https://github.com/voska/qbo-cli"
    description: "QuickBooks Online CLI for humans and AI agents"
    license: "MIT"
    repository:
      owner: voska
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: voska-bot
      email: bot@voska.dev
    install: |
      bin.install "qbo"
    test: |
      system "#{bin}/qbo", "exit-codes", "--json"

scoops:
  - name: qbo
    homepage: "https://github.com/voska/qbo-cli"
    description: "QuickBooks Online CLI for humans and AI agents"
    license: "MIT"
    repository:
      owner: voska
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"

nfpms:
  - package_name: qbo
    homepage: "https://github.com/voska/qbo-cli"
    maintainer: "voska <hello@voska.dev>"
    description: "QuickBooks Online CLI for humans and AI agents"
    license: "MIT"
    formats:
      - deb
      - rpm
